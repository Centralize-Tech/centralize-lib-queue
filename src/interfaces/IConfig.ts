export interface IConfig {
  host?: string,
  name?: string,
  port?:  string,
  user?: string,
  passwd?: string,
  vhost?: string,
  useSSL?: string,
  consolePort?: string,
  consoleUser?: string,
  consolePasswd?: string,
  connectionHeartbeat?: string,
}
